name: Release

on:
  release:
    types: [published]

jobs:
  publish:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write
    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4
        with:
          version: 9

      - uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "pnpm"
          registry-url: "https://registry.npmjs.org"

      - run: pnpm install --frozen-lockfile

      - run: pnpm lint

      - run: pnpm typecheck

      - run: pnpm test

      - run: pnpm build

      - name: Check API compatibility
        run: pnpm api:check
        working-directory: packages/seatmaps

      # release-please already bumped package.json via the Release PR,
      # so no version stamping step is needed here.

      - name: Publish to npm
        run: pnpm publish --access public --no-git-checks
        working-directory: packages/seatmaps
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
